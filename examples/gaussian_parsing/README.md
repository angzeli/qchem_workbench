# Gaussian Parsing Example

This example contains a synthetic Gaussian-like output fixture. It is intended
only to demonstrate qchem-workbench parsing and reporting commands; it is not a
real Gaussian calculation and should not be used for scientific conclusions.

Example command:

```bash
qchemwb parse-gaussian outputs/ --out gaussian_results.json --csv gaussian_results.csv
```

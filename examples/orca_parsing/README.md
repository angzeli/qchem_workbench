# ORCA Parsing Example

This example contains a synthetic ORCA-like output fixture. It demonstrates
input-free parsing of ORCA text output into the generic result schema. ORCA is
not run by this package or required for this example.

Example command:

```bash
qchemwb parse-orca outputs/ --out orca_results.json --csv orca_results.csv
```

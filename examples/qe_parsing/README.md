# Quantum ESPRESSO Parsing Example

This example contains a synthetic Quantum ESPRESSO `pw.x`-like output fixture.
It demonstrates parser behavior without requiring Quantum ESPRESSO.

Example command:

```bash
qchemwb parse-qe outputs/ --out qe_results.json --csv qe_results.csv
```
